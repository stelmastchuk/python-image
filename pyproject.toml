[project]
name = "python-image"
version = "0.1.0"
description = "Api teste"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
]

[dependency-groups]
dev = [
    "pylint>=4.0.6",
    "pytest>=9.1.1",
]
